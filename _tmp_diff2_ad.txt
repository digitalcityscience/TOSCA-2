+    /**
+     * Stops the active tracking display. Idempotent. Only tears down `MockTrackingSource` — the
+     * real Python transport (ticket 08) is mount-owned and stays connected regardless of this
+     * button, since "Connected" and "tracking actively displayed" are separate states.
+     */
     function stopMockTracking(): void {
         mockSource?.stop();
         mockSource = undefined;
-        realSource?.stop();
-        realSource = undefined;
         active.value = false;
         // An intentional stop is not a tracking-availability problem — clear any stale
         // suppressed/disconnected banner left over from before the operator stopped tracking.
         trackingAvailability.value = "live";
-        pythonConnectionState.value = "mock";
-        detectedReferenceMarkerIds.value = new Set();
     }
 
-    /** Tears down the active tracking source and the render watch. Idempotent — safe on unmount and HMR. */
+    /** Tears down every tracking source, the Python transport, and the render watch. Idempotent — safe on unmount and HMR. */
     function stop(): void {
         stopWatch?.();
         stopWatch = undefined;
         stopMockTracking();
+        disconnectPythonTransport();
         appliedRotationByObjectId.clear();
     }
 
@@ -732,9 +796,11 @@ export const useCollabTrackingRenderStore = defineStore("collabTrackingRender",
         trackingAvailability,
         pythonConnectionState,
         detectedReferenceMarkerIds,
+        mapCalibrationMarkerHealth,
         startRendering,
         startMockTracking,
         stopMockTracking,
+        retryPythonConnection,
         stop,
     };
 });
diff --git a/src/locales/de.json b/src/locales/de.json
index 15c8455..4c3cb9d 100644
--- a/src/locales/de.json
+++ b/src/locales/de.json
@@ -369,9 +369,22 @@
         "connected": "Verbunden",
         "reconnecting": "Verbindet erneut…",
         "disconnected": "Getrennt",
+        "retry": "Erneut versuchen",
+        "connectionLost": "Verbindung zum Python-Tracking-Server verloren — zum erneuten Versuch klicken",
         "markers": {
           "title": "Referenzmarker",
-          "camera": "Kamera {cameraId}"
+          "camera": "Kamera {cameraId}",
+          "calibration": {
+            "title": "Kartenkalibrierungsmarker",
+            "detected": "erkannt",
+            "waiting": "wartet",
+            "corners": {
+              "top_left": "oben links",
+              "top_right": "oben rechts",
+              "bottom_left": "unten links",
+              "bottom_right": "unten rechts"
+            }
+          }
         }
       },
       "simulation": {
diff --git a/src/locales/en.json b/src/locales/en.json
index 311fe7a..89310ba 100644
--- a/src/locales/en.json
+++ b/src/locales/en.json
@@ -369,9 +369,22 @@
         "connected": "Connected",
         "reconnecting": "Reconnecting…",
         "disconnected": "Disconnected",
+        "retry": "Retry",
+        "connectionLost": "Lost connection to the Python tracking server — click Retry to try again",
         "markers": {
           "title": "Reference markers",
-          "camera": "Camera {cameraId}"
+          "camera": "Camera {cameraId}",
+          "calibration": {
+            "title": "Map calibration markers",
+            "detected": "detected",
+            "waiting": "waiting",
+            "corners": {
+              "top_left": "top-left",
+              "top_right": "top-right",
+              "bottom_left": "bottom-left",
+              "bottom_right": "bottom-right"
+            }
+          }
         }
       },
       "simulation": {
diff --git a/src/locales/tr.json b/src/locales/tr.json
index 530d92e..01d4f1a 100644
--- a/src/locales/tr.json
+++ b/src/locales/tr.json
@@ -369,9 +369,22 @@
         "connected": "Bağlı",
         "reconnecting": "Yeniden bağlanıyor…",
         "disconnected": "Bağlantı yok",
+        "retry": "Yeniden dene",
+        "connectionLost": "Python takip sunucusuyla bağlantı kesildi — yeniden denemek için tıklayın",
         "markers": {
           "title": "Referans işaretçiler",
-          "camera": "Kamera {cameraId}"
+          "camera": "Kamera {cameraId}",
+          "calibration": {
+            "title": "Harita kalibrasyon işaretçileri",
+            "detected": "algılandı",
+            "waiting": "bekleniyor",
+            "corners": {
+              "top_left": "sol üst",
+              "top_right": "sağ üst",
+              "bottom_left": "sol alt",
+              "bottom_right": "sağ alt"
+            }
+          }
         }
       },
       "simulation": {
